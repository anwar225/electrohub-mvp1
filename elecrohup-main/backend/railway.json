{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeploy": "npx prisma generate && npx prisma migrate deploy",
    "startCommand": "npm start",
    "healthcheckPath": "/health"
  }
}